export interface Food {

    _id: string,
    type?: string,
    name: string,
    is_vegan?: boolean,
    city_district?: string,
    address?: string,
    phone_number?: string,
    website?: string,
}